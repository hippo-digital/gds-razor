@using GdsRazor.Models

@{
    ViewData["Title"] = "WarningText Demo";
    Layout = "../Layout";
}

<h1 class="govuk-heading-l">WarningText</h1>
<h2 class="govuk-heading-s">Default:</h2>
<div class="demo">
    @await Html.PartialAsync("GdsWarningText", new WarningTextModel())
</div>

<h2 class="govuk-heading-s">Custom:</h2>
<div class="demo">
    @await Html.PartialAsync("GdsWarningText", new WarningTextModel(
        "You can be fined up to £5,000 if you do not register."
    ) {
        IconFallbackText = "Warning"
    })
</div>
